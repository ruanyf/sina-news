name: Vercel-cron
on:
  push:
    branches:
      - main
  schedule:
    - cron: '1/28 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel deploy webhook
        run: |
          curl -X POST ${{ secrets.VERCEL_HOOKS }}
